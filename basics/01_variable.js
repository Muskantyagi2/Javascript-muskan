/*
for memeory management - variable and const 

constant - const ( cant chnge value after defining)
variable - var & let ( can change value )

but not prefer var ( because of the problm of block scope and fucntion scope )
and use let for it 

*/

const accountId = 123
let accountEmail = "muskan@gmail.com"
var accountPassword = "mypassword"
accountName = "muskan"

let accountState;

console.table([accountId, accountEmail, accountPassword, accountName, accountState])